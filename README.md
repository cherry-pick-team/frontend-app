# web app for cherry-pick

```shell
npm i                          # install deps
gulp                           # run in development
gulp --type production         # run like in production
gulp build --type production   # build for production (./dist)
```
